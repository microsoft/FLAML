
---
Vanilla:
  is_naive: True
  config:


Oracle:
  is_naive: True 
  config:
    q: ['ab','ac','cd']

Oracle22:
  is_naive: True 
  config:
    q: ['ab','cd','ac']

Oracle1:
  is_naive: True 
  config:
    q: ['eh']

Oracle2:
  is_naive: True 
  config:
    q: ['fj']

naive_oracle2:
  is_naive: True 
  config:
    q: ['fj']

Oracle3:
  is_naive: True 
  config:
    q: ['ae']

Oracle4:
  is_naive: True 
  config:
    q: ['cg']

Random:
  is_naive: False
  config:
    # max_live_model_num: None
    min_resource_lease: .inf
    model_select_policy: 'threshold_loss_ucb'
    automl_runner_args: 
      champion_test_policy: null
      
Exhaustive:
  is_naive: False
  config:
    max_live_model_num: 50
    min_resource_lease: .inf
    model_select_policy: 'threshold_loss_ucb'
    automl_runner_args: 
      champion_test_policy: null
      

# ChaCha_basic_config: &ChaCha_basic_config
#   remove_worse: 1

# ChaCha:
#   is_naive: False
#   config:
#     # max_live_model_num: None # None means we set concurrent running budget as required
#     model_select_policy: 'threshold_loss_ucb'
#     scheduler_args:
#       keep_challenger_metric: 'ucb'
#       keep_challenger_ratio: 0.5
#       keep_champion: True
#     automl_runner_args: 
#       champion_test_policy: 'loss_ucb'
#       remove_worse: False

ChaCha:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True

ChaCha_pause_old:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args: 
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
      pause_old_froniter: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: False

ChaCha_remove:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True

ChaCha_001remove:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True
    cb_coef: 0.01
 
ChaCha_pauseremove:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
      pause_old_froniter: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True

ChaCha_001pauseremove:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
      pause_old_froniter: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True
    cb_coef: 0.01

ChaCha_cb001:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: False
    cb_coef: 0.01

ChaCha_cb003:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args:
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: False
    cb_coef: 0.03

# ChaCha_cb01:
#   is_naive: False
#   config:
#     # max_live_model_num: None # None means we set concurrent running budget as required
#     model_select_policy: 'threshold_loss_ucb'
#     scheduler_args:
#       keep_challenger_metric: 'ucb'
#       keep_challenger_ratio: 0.5
#       keep_champion: True
#     automl_runner_args: 
#       champion_test_policy: 'loss_ucb'
#       remove_worse: False
#     cb_coef: 0.1

ChaCha_wo_champion:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args: 
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: 0.5
      keep_champion: False
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True   

ChaCha_aggressive:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args: 
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: null
      keep_champion: True
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True      

ChaCha_wo_champion_aggressive:
  is_naive: False
  config:
    # max_live_model_num: None # None means we set concurrent running budget as required
    model_select_policy: 'threshold_loss_ucb'
    scheduler_args: 
      keep_challenger_metric: 'ucb'
      keep_challenger_ratio: null
      keep_champion: False
    automl_runner_args: 
      champion_test_policy: 'loss_ucb'
      remove_worse: True     
# ChaCha_w_l:
#     trial_runner_name: ChaCha-L'
#     hp_to_tune: 
#       interactions: {} 
#       learning_rate: 0.5

# ChaCha = {
#     "trial_runner_name": 'ChaCha-Org',
#     'remove_worse': 1,
#     'hp_to_tune': hp_to_tune,
#     }

# ChaCha_debug = {
#     "trial_runner_name": 'ChaCha-Debug',
#     'remove_worse': 1,
#     'hp_to_tune': hp_to_tune,
#     }

# ChaCha_w_l = {
#     "trial_runner_name": 'ChaCha-L',
#     'remove_worse': 1,
#     'hp_to_tune': hp_to_tune_with_l,
#     }


# ChaCha_demo = {
#     "trial_runner_name": 'ChaCha-Demo',
#     'remove_worse': 1,
#     }

# ChaCha_keep_0 = {
#     "trial_runner_name": 'ChaCha-ucb-top0',
#     'remove_worse': 1,
#     }
# ChaCha_no_champion = {
#     "trial_runner_name": 'ChaCha-nochampion-top0',
#     'remove_worse': 1,
#     }

# ChaCha_CB = {
#     "trial_runner_name": 'ChaCha-CB',
#     'remove_worse': 1,
#     }

# Chambent_vanilla = {
#     "trial_runner_name": 'Chambent-Vanilla',
#     'remove_worse': 1,
#     }

# Chambent_vanilla_champion = {
#     "trial_runner_name": 'Chambent-champion-Vanilla',
#     'remove_worse': 1,
#     }

# Chambent_van_top1 = {
#     "trial_runner_name": 'Chambent-Van-ucb-top1',
#     'remove_worse': 1,
#     }

# Chambent_van_tophalf = {
#     "trial_runner_name": 'Chambent-Van-ucb-tophalf',
#     'remove_worse': 1,
#     }


# Chambent_van_top1_lcb = {
#     "trial_runner_name": 'Chambent-Van-lcb-top1',
#     'remove_worse': 1,
#     }

# Chambent_van_tophalf_lcb = {
#     "trial_runner_name": 'Chambent-Van-lcb-tophalf',
#     'remove_worse': 1,
#     }


# Chambent_van_tophalf_avg = {
#     "trial_runner_name": 'Chambent-Van-avg-tophalf',
#     'remove_worse': 1,
#     }
    
# Chambent_van_tophalf_avg_champion = {
#     "trial_runner_name": 'Chambent-Van-avg-champion-tophalf',
#     'remove_worse': 1,
#     }

# Chambent_van_tophalf_champion = {
#     "trial_runner_name": 'Chambent-Van-ucb-champion-tophalf',
#     'remove_worse': 1,
#     }

# Chambent_van_tophalf_champion_inc = {
#     "trial_runner_name": 'Chambent-Van-ucb-championINC-tophalf',
#     'model_select_policy': 'select:threshold_ucb_champion',
#     'remove_worse': 1,
#     }
# Chambent_test = {
#     "trial_runner_name": 'Chambent-Test',
#     'remove_worse': 1,
#     }
# Chambent_noreset = {
#     "trial_runner_name": 'Chambent-Noreset',
#     'remove_worse': 1,
#     }
# Chambent_pause = {
#     "trial_runner_name": 'Chambent-Pause',
#     'remove_worse': 1,
#     }
# Chambent_Inf = {
#     "trial_runner_name": 'Chambent-Inf',
#     'keep_all_running': 1,
#     'remove_worse': 1,
#     }
